.columns
  .column
    .card
      .card-content.content
        %h2
          = link_to "#{category.name}の美容整形・悩み", category_path(category)
        %p #{category.name}の美容整形別に、効果や施術の流れ・リスク(失敗例や副作用)・値段の相場を中心に、ダウンタイム(術後経過・痛み・腫れ・内出血・赤み)や口コミ・名医・おすすめクリニックなどを紹介しています。
        %ul.is-marginless.columns.is-multiline.is-mobile
          = render partial: 'category_list', collection: category.children
